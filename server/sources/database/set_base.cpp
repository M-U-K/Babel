/*
** EPITECH PROJECT, 2021
** Delivery
** File description:
** set_base
*/

#include "Database.hpp"
#include <cassert>
using namespace sqlite_orm;
using namespace babel;

/*
void Database::set_name(int& id, std::string& name)
{
    storage.update_all(sqlite_orm::set(c(&User::name) = name), where(c(&User::id) == id));
}

void Database::set_psw(int& id, std::string& psw)
{
    storage.update_all(sqlite_orm::set(c(&User::psw) = psw), where(c(&User::id) == id));
}

void Database::set_status(int& id, bool& status)
{
    storage.update_all(sqlite_orm::set(c(&User::status) = status), where(c(&User::id) == id));
}

void Database::set_pp(int& id, std::string& pp)
{
    storage.update_all(set(c(&User::pp) = pp), where(c(&User::id) == id));
}
*/